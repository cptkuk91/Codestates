let words = ['Radagast', 'the', 'Brown'];

// elements 를 찾고자 할 때는 includes를 사용하면 됩니다.

console.log(words.includes('the'));
// Includes를 통해 배열 내 특정 element 존재 여부를 알 수 있다.

// includes 는 indexOf() === -1 보다 편하지만
// 호환성에 문제가 있기 때문에 indexOf 가 더 중요하다.

