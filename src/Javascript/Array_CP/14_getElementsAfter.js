function getElementsAfter(arr, n) {
    // TODO: 여기에 코드를 작성합니다.
    let arr2 = [];
    if(arr2 >= arr.length) {
        return arr2;
    }

    return arr.slice(n + 1);
}

// function getElementsAfter(arr, n) {
//     // TODO: 여기에 코드를 작성합니다.
//     // 어려움
//     return arr.slice(n + 1);
// }
