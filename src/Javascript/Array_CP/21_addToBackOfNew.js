function addToBackOfNew(arr, el) {
    let arr2 = arr.slice();
    arr2.push(el);
    return arr2;
}

// function addToBackOfNew(arr, el) {
//     // TODO: 여기에 코드를 작성합니다.
//     return arr2 = arr.concat(el);
// }
