function getLengthOfElements(arr) {
    // TODO: 여기에 코드를 작성합니다.
    // map을 사용해라
    // 문자열의 길이를 새로운 배열을 통해 리턴
    // arr 각 문자열의 길이를 구한다?
    // 문자열 길이
    // 새로운 배열
    // 리턴?
    let result = arr.map(function(el){
        return el.length;
    });
    return result;
}
